#ifndef WAVESHAPERS_DOT_H
#define  WAVESHAPERS_DOT_H

namespace waveshapers
{ 
  float tri(const float v,const float amt);
  float sin(const float v,const float amt);
  float cheby(const float v, const float amt);
  float sigm(const float v,const float amt);
}

#endif
